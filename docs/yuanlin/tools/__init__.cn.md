# __init__.py - Python 包初始化文件

> 源文件: `tools/__init__.py`

## 概述

空的 `__init__.py` 文件,将 `tools/` 目录标记为 Python 包,允许其中的模块被其他 Python 脚本导入。

## 架构位置

Python 包系统的标准标记文件。

## 内部实现细节

文件为空,仅起包标记作用。

## 依赖关系

无。

## 相关文件

- `tools/` 目录下的其他 Python 模块
