# rm.py - 跨平台文件删除

> 源文件: `gn/rm.py`

## 概述
跨平台的文件/目录删除脚本,用于 GN 构建系统中的清理操作。

## 架构位置
Skia GN 构建系统的文件操作工具。

## 内部实现细节
检查目标是否存在,目录用 `shutil.rmtree` 删除,文件用 `os.remove` 删除。目标不存在时静默退出。

## 相关文件
- `gn/cp.py`
