# yq_mac_arm64 - macOS ARM64 yq 工具

## 概述

macOS ARM64 平台的 yq YAML 处理器资源。

## 目录结构

```
yq_mac_arm64/
├── create.py   # 自动化创建脚本
└── VERSION     # 当前版本号
```

## 依赖关系

- 被 macOS ARM64 机器上需要 YAML 处理的任务使用

## 相关文档与参考

- `yq/` - 通用版本
