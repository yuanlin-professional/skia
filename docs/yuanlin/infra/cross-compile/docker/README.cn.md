# cross-compile/docker - 交叉编译 Docker 配置

## 概述

交叉编译所需的 Docker 镜像定义目录。

## 目录结构

```
docker/
└── cross-linux-arm64/   # Linux ARM64 交叉编译环境
    └── Dockerfile
```

## 相关文档与参考

- 父目录 `cross-compile/` 说明
