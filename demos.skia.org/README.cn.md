要使用 CanvasKit 的本地构建来运行这些演示，请进入 CanvasKit 文件夹
`//modules/canvaskit` 并运行 `make debug` 或类似命令来构建它。

然后，从本目录运行 `make local` 来启动本地 Web 服务器。

你需要修改你的演示以从本地 Web 服务器加载，而不是从 CDN 加载。请参阅 ./demos/hello_world 中的示例。
